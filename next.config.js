/** @type {import('next').NextConfig} */
const nextConfig = {
    reactStrictMode: true,
    images: {
        domains: ['randomuser.me', 'i.pravatar.cc', 'ui-avatars.com'],
    },
}

module.exports = nextConfig
